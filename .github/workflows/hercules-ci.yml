name: "Hercules CI"
run-name: "Hercules CI"

on:
  push:
  workflow_dispatch:

permissions:
  contents: read
  statuses: read

# GitHub-hosted HCI jobs consume the GHCI cache lanes; only GitHub Cache
# Maintenance writes them.
jobs:
  github-aarch64-darwin-agent:
    name: "GitHub aarch64-darwin HCI Agent"
    runs-on: macos-26
    timeout-minutes: 355
    env:
      HCI_AGENT_HOSTNAME: github-aarch64-darwin
      HCI_AGENT_READY_TARGET_ATTR: .#darwinConfigurations.andred.config.system.build.toplevel
      HCI_AGENT_READY_TARGET_NAME: andred
      HCI_AGENT_SYSTEM: aarch64-darwin
      NIX_ROOT_CACHE_KEY_PREFIX: nix-root-ci-aarch64-darwin-sparsebundle-v1
      NIX_ROOT_IMAGE_DIR_NAME: nix-root-ci-aarch64-darwin
      NIX_ROOT_IMAGE_SIZE: 64g
      NIX_ROOT_SYSTEM: aarch64-darwin
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Restore sparsebundle Nix root cache
        uses: ./.github/actions/nix-sparsebundle-cache-restore
        with:
          cache-key-prefix: ${{ env.NIX_ROOT_CACHE_KEY_PREFIX }}
          cache-key-suffix: agent
          image-dir-name: ${{ env.NIX_ROOT_IMAGE_DIR_NAME }}
          system: ${{ env.NIX_ROOT_SYSTEM }}

      - name: Mount sparsebundle Nix root
        uses: ./.github/actions/nix-sparsebundle-mount
        with:
          image-size: ${{ env.NIX_ROOT_IMAGE_SIZE }}
          system: ${{ env.NIX_ROOT_SYSTEM }}

      - name: Install Nix
        uses: ./.github/actions/install-nix
        with:
          extra-nix-conf: |
            narinfo-cache-negative-ttl = 0
            max-jobs = auto

      - name: Run ephemeral Hercules CI agent
        id: hci-agent
        shell: bash
        env:
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_CACHE_NAME: whitestrake
          GITHUB_TOKEN: ${{ github.token }}
          HERCULES_CI_CLUSTER_JOIN_TOKEN: ${{ secrets.HERCULES_CI_CLUSTER_JOIN_TOKEN }}
          HERCULES_CI_CREDENTIALS_JSON: ${{ secrets.HERCULES_CI_CREDENTIALS_JSON }}
          HCI_AGENT_JOB_NAME_FILE: ${{ runner.temp }}/hci-final-gate-job-name
          HCI_AGENT_TIMEOUT_SECONDS: "18000"
          HCI_PROJECT: github/whitestrake/nixos
        run: |
          set -euo pipefail

          bash .github/scripts/hci-agent-supervisor.sh

  github-aarch64-linux-agent:
    name: "GitHub aarch64-linux HCI Agent"
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 355
    env:
      GHCRTS: -xr128G
      HCI_AGENT_HOSTNAME: github-aarch64-linux
      HCI_AGENT_READY_TARGET_ATTR: .#nixosConfigurations.jaeger.config.system.build.toplevel
      HCI_AGENT_READY_TARGET_NAME: jaeger
      HCI_AGENT_SYSTEM: aarch64-linux
      NIX_ROOT_CACHE_KEY_PREFIX: nix-root-ci-aarch64-linux-ext4-v1
      NIX_ROOT_IMAGE_DIR_NAME: nix-root-ci-aarch64-linux
      NIX_ROOT_IMAGE_SIZE: 32G
      NIX_ROOT_SYSTEM: aarch64-linux
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Restore ext4 Nix root cache
        uses: ./.github/actions/nix-ext4-cache-restore
        with:
          cache-key-prefix: ${{ env.NIX_ROOT_CACHE_KEY_PREFIX }}
          cache-key-suffix: agent
          image-dir-name: ${{ env.NIX_ROOT_IMAGE_DIR_NAME }}
          system: ${{ env.NIX_ROOT_SYSTEM }}

      - name: Mount ext4 Nix root
        uses: ./.github/actions/nix-ext4-mount
        with:
          image-size: ${{ env.NIX_ROOT_IMAGE_SIZE }}
          system: ${{ env.NIX_ROOT_SYSTEM }}

      - name: Install Nix
        uses: ./.github/actions/install-nix
        with:
          extra-nix-conf: |
            narinfo-cache-negative-ttl = 0
            max-jobs = auto

      - name: Run ephemeral Hercules CI agent
        id: hci-agent
        shell: bash
        env:
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_CACHE_NAME: whitestrake
          GITHUB_TOKEN: ${{ github.token }}
          HERCULES_CI_CLUSTER_JOIN_TOKEN: ${{ secrets.HERCULES_CI_CLUSTER_JOIN_TOKEN }}
          HERCULES_CI_CREDENTIALS_JSON: ${{ secrets.HERCULES_CI_CREDENTIALS_JSON }}
          HCI_AGENT_JOB_NAME_FILE: ${{ runner.temp }}/hci-final-gate-job-name
          HCI_AGENT_TIMEOUT_SECONDS: "18000"
          HCI_PROJECT: github/whitestrake/nixos
        run: |
          set -euo pipefail

          bash .github/scripts/hci-agent-supervisor.sh


  namespace-aarch64-darwin-artifact-agent:
    name: "Namespace aarch64-darwin Artifact HCI Agent"
    runs-on: namespace-profile-aarch64-darwin-no-cache
    timeout-minutes: 355
    concurrency:
      group: hci-agent-runner-namespace-aarch64-darwin
      cancel-in-progress: false
    env:
      NAMESPACE_ARTIFACT_API_URL: https://ord.storage.namespaceapis.com
      NAMESPACE_NIX_STORE_ARTIFACT: hci-darwin-agent/aarch64-darwin-nix-root.sparsebundle.tar.zst
      NAMESPACE_NIX_STORE_ARTIFACT_EXPIRY: 720h
      NAMESPACE_NIX_STORE_IMAGE_SIZE: 64g
      HCI_AGENT_HOSTNAME: namespace-aarch64-darwin
      HCI_AGENT_READY_TARGET_ATTR: .#darwinConfigurations.andred.config.system.build.toplevel
      HCI_AGENT_READY_TARGET_NAME: andred
      HCI_AGENT_SYSTEM: aarch64-darwin
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Prepare Namespace sparsebundle Nix root
        id: namespace-store
        shell: bash
        env:
          NAMESPACE_BEARER_TOKEN: ${{ secrets.NAMESPACE_BEARER_TOKEN }}
        run: |
          set -euo pipefail

          namespace_bearer_token() {
            local token_file="${NSC_TOKEN_FILE:-/var/run/nsc/token.json}"

            if [ -r "$token_file" ]; then
              plutil -extract bearer_token raw "$token_file"
              return 0
            fi

            if [ -n "${NAMESPACE_BEARER_TOKEN:-}" ]; then
              printf '%s\n' "$NAMESPACE_BEARER_TOKEN"
              return 0
            fi

            return 1
          }

          resolve_artifact_url() {
            local artifact_path="$1"
            local output_path="$2"
            local token
            local request_path

            token="$(namespace_bearer_token)" || return 1
            request_path="$image_parent/resolve-request.json"

            printf '{"path":"%s","namespace":"main"}\n' "$artifact_path" > "$request_path"

            printf 'Authorization: Bearer %s\n' "$token" |
              curl --fail --location --show-error --silent \
                --header @- \
                --header "Content-Type: application/json" \
                --header "Connect-Protocol-Version: 1" \
                --data-binary @"$request_path" \
                "$NAMESPACE_ARTIFACT_API_URL/namespace.cloud.storage.v1beta.ArtifactsService/ResolveArtifact" \
                > "$output_path"
          }

          synthetic_conf=/etc/synthetic.conf
          image_parent="${RUNNER_TEMP:-${TMPDIR:-/tmp}}/hci-darwin-nix-store"
          image_bundle="$image_parent/nix-root.sparsebundle"
          image_archive="$image_parent/nix-root.sparsebundle.tar.zst"
          resolve_response="$image_parent/resolve-response.json"
          restored=false

          mkdir -p "$image_parent"
          rm -rf "$image_bundle" "$image_archive" "$resolve_response"

          {
            printf 'NAMESPACE_NIX_STORE_IMAGE_BUNDLE=%s\n' "$image_bundle"
            printf 'NAMESPACE_NIX_STORE_IMAGE_ARCHIVE=%s\n' "$image_archive"
          } >> "$GITHUB_ENV"

          if zstd_path="$(command -v zstd 2>/dev/null)"; then
            zstd_version="$("$zstd_path" --version 2>/dev/null || true)"
            echo "NAMESPACE_ARTIFACT_ZSTD_LOCAL available=true path=$zstd_path version=$zstd_version"
          else
            echo "NAMESPACE_ARTIFACT_ZSTD_LOCAL available=false"
            echo "::error ::zstd is required for the Namespace sparsebundle artifact experiment."
            exit 1
          fi

          sudo touch "$synthetic_conf"

          if grep -qE '^nix($|[[:space:]])' "$synthetic_conf"; then
            if ! grep -qx 'nix' "$synthetic_conf"; then
              echo "::error ::$synthetic_conf already defines nix differently."
              grep -nE '^nix($|[[:space:]])' "$synthetic_conf" || true
              exit 1
            fi
          else
            printf 'nix\n' | sudo tee -a "$synthetic_conf" >/dev/null
          fi

          if ! sudo /System/Library/Filesystems/apfs.fs/Contents/Resources/apfs.util -B >/dev/null 2>&1; then
            sudo /System/Library/Filesystems/apfs.fs/Contents/Resources/apfs.util -t >/dev/null 2>&1 \
              || echo "apfs.util did not report successful synthetic object materialization; validating /nix directly."
          fi

          if [ ! -e /nix ]; then
            echo "::error ::macOS did not materialize synthetic /nix."
            sudo cat "$synthetic_conf"
            exit 1
          fi

          if [ -L /nix ]; then
            echo "::error ::/nix is a symlink; Nix requires /nix to be a real directory or mountpoint."
            ls -ld /nix
            exit 1
          fi

          started="$SECONDS"
          if resolve_artifact_url "$NAMESPACE_NIX_STORE_ARTIFACT" "$resolve_response"; then
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_IMAGE_RESOLVE_HIT path=$NAMESPACE_NIX_STORE_ARTIFACT durationSeconds=$duration"

            started="$SECONDS"
            signed_download_url="$(plutil -extract signedDownloadUrl raw "$resolve_response")"
            curl --fail --location --show-error --silent "$signed_download_url" \
              | zstd -dc \
              | tar -xpf - -C "$image_parent"
            restored=true
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_IMAGE_RESTORE_STREAMED path=$NAMESPACE_NIX_STORE_ARTIFACT format=sparsebundle-tar compression=zstd durationSeconds=$duration"
          elif command -v nsc >/dev/null 2>&1 && nsc artifact download "$NAMESPACE_NIX_STORE_ARTIFACT" "$image_archive"; then
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_IMAGE_RESTORE_HIT path=$NAMESPACE_NIX_STORE_ARTIFACT durationSeconds=$duration"
            ls -lh "$image_archive"

            started="$SECONDS"
            zstd -dc "$image_archive" | tar -xpf - -C "$image_parent"
            restored=true
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_IMAGE_RESTORE_EXTRACTED path=$NAMESPACE_NIX_STORE_ARTIFACT format=sparsebundle-tar compression=zstd durationSeconds=$duration"
          else
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_IMAGE_RESTORE_MISS path=$NAMESPACE_NIX_STORE_ARTIFACT durationSeconds=$duration"

            started="$SECONDS"
            hdiutil create \
              -size "$NAMESPACE_NIX_STORE_IMAGE_SIZE" \
              -type SPARSEBUNDLE \
              -fs 'Case-sensitive Journaled HFS+' \
              -volname NixStore \
              "$image_bundle"
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_IMAGE_CREATED path=$image_bundle size=$NAMESPACE_NIX_STORE_IMAGE_SIZE fs=case-sensitive-jhfs-plus durationSeconds=$duration"
          fi

          if [ ! -d "$image_bundle" ]; then
            echo "::error ::Namespace Nix sparsebundle is missing at $image_bundle."
            ls -la "$image_parent"
            exit 1
          fi

          if [ "$restored" = true ]; then
            started="$SECONDS"
            hdiutil resize -size "$NAMESPACE_NIX_STORE_IMAGE_SIZE" "$image_bundle"
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_IMAGE_RESIZED path=$image_bundle size=$NAMESPACE_NIX_STORE_IMAGE_SIZE durationSeconds=$duration"
          fi

          started="$SECONDS"
          sudo hdiutil attach \
            -mountpoint /nix \
            -nobrowse \
            "$image_bundle"
          duration="$((SECONDS - started))"
          echo "NAMESPACE_ARTIFACT_IMAGE_ATTACHED path=$image_bundle mountPoint=/nix durationSeconds=$duration"

          printf 'mounted=true\n' >> "$GITHUB_OUTPUT"
          mounted_device="$(diskutil info -plist /nix | plutil -extract DeviceNode raw -)"
          case "$mounted_device" in
            /dev/disk*) ;;
            *)
              echo "::error ::unexpected Namespace sparsebundle device: $mounted_device"
              exit 1
              ;;
          esac
          printf 'mounted-device=%s\n' "$mounted_device" >> "$GITHUB_OUTPUT"

          actual_bytes="$(diskutil info -plist /nix | plutil -extract TotalSize raw -)"
          minimum_bytes=$((64 * 1024 * 1024 * 1024 * 99 / 100))
          if ! [[ "$actual_bytes" =~ ^[1-9][0-9]*$ ]] || [ "$actual_bytes" -lt "$minimum_bytes" ]; then
            echo "::error ::mounted Namespace sparsebundle capacity is below $NAMESPACE_NIX_STORE_IMAGE_SIZE: actualBytes=${actual_bytes:-unknown}"
            exit 1
          fi
          echo "NAMESPACE_ARTIFACT_IMAGE_CAPACITY_VERIFIED path=$image_bundle actualBytes=$actual_bytes requestedSize=$NAMESPACE_NIX_STORE_IMAGE_SIZE"

          sudo chown "$USER" /nix
          chmod u+rwx /nix

          echo "Mounted Namespace sparsebundle $image_bundle at /nix"

      - name: Install Nix
        uses: ./.github/actions/install-nix
        with:
          extra-nix-conf: |
            narinfo-cache-negative-ttl = 0
            max-jobs = auto

      - name: Run ephemeral Hercules CI agent
        id: hci-agent
        shell: bash
        env:
          CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}
          CACHIX_CACHE_NAME: whitestrake
          GITHUB_TOKEN: ${{ github.token }}
          HERCULES_CI_CLUSTER_JOIN_TOKEN: ${{ secrets.HERCULES_CI_CLUSTER_JOIN_TOKEN }}
          HERCULES_CI_CREDENTIALS_JSON: ${{ secrets.HERCULES_CI_CREDENTIALS_JSON }}
          HCI_AGENT_JOB_NAME: 10-darwinConfiguration-andred
          HCI_AGENT_TIMEOUT_SECONDS: "18000"
          HCI_PROJECT: github/whitestrake/nixos
        run: |
          set -euo pipefail

          bash .github/scripts/hci-agent-supervisor.sh

      - name: Maintain HCI agent cache roots
        id: namespace-root-maintenance
        if: ${{ always() && steps.namespace-store.outcome == 'success' && steps.hci-agent.outputs.job-found != 'false' }}
        uses: ./.github/actions/hci-root-maintenance
        with:
          ready-target-attr: ${{ env.HCI_AGENT_READY_TARGET_ATTR }}
          ready-target-name: ${{ env.HCI_AGENT_READY_TARGET_NAME }}
          system: ${{ env.HCI_AGENT_SYSTEM }}

      - name: Garbage collect Nix root
        id: namespace-root-gc
        if: ${{ always() && steps.namespace-store.outcome == 'success' && steps.hci-agent.outputs.job-found != 'false' }}
        uses: ./.github/actions/nix-root-gc
        with:
          system: ${{ env.HCI_AGENT_SYSTEM }}

      - name: Detach Namespace sparsebundle Nix root
        id: namespace-store-detach
        if: ${{ always() && steps.namespace-store.outputs.mounted == 'true' }}
        shell: bash
        env:
          MOUNTED_DEVICE: ${{ steps.namespace-store.outputs.mounted-device }}
        run: |
          set -euo pipefail

          current_device="$(diskutil info -plist /nix | plutil -extract DeviceNode raw -)"
          if [ -n "$MOUNTED_DEVICE" ] && [ "$current_device" != "$MOUNTED_DEVICE" ]; then
            echo "::error ::/nix is mounted from $current_device, expected $MOUNTED_DEVICE."
            exit 1
          fi
          MOUNTED_DEVICE="$current_device"

          case "$MOUNTED_DEVICE" in
            /dev/disk*) ;;
            *)
              echo "::error ::refusing to detach unexpected device: $MOUNTED_DEVICE"
              exit 1
              ;;
          esac

          started="$SECONDS"
          sync
          sudo hdiutil detach "$MOUNTED_DEVICE" || sudo hdiutil detach -force "$MOUNTED_DEVICE"
          duration="$((SECONDS - started))"
          echo "NAMESPACE_ARTIFACT_IMAGE_DETACHED device=$MOUNTED_DEVICE mountPoint=/nix durationSeconds=$duration"

      - name: Save Namespace sparsebundle Nix root artifact
        if: ${{ always() && steps.namespace-store.outcome == 'success' && steps.namespace-root-maintenance.outcome == 'success' && steps.namespace-root-gc.outcome == 'success' && steps.namespace-store-detach.outcome == 'success' }}
        shell: bash
        run: |
          set -euo pipefail

          image_bundle="${NAMESPACE_NIX_STORE_IMAGE_BUNDLE:-${RUNNER_TEMP:-${TMPDIR:-/tmp}}/hci-darwin-nix-store/nix-root.sparsebundle}"
          image_archive="${NAMESPACE_NIX_STORE_IMAGE_ARCHIVE:-${RUNNER_TEMP:-${TMPDIR:-/tmp}}/hci-darwin-nix-store/nix-root.sparsebundle.tar.zst}"
          image_parent="$(dirname "$image_bundle")"

          if ! command -v nsc >/dev/null 2>&1; then
            echo "::error ::nsc CLI is not available."
            exit 1
          fi

          if [ ! -d "$image_bundle" ]; then
            echo "::error ::Namespace Nix sparsebundle is missing at $image_bundle."
            ls -la "$image_parent"
            exit 1
          fi

          du -sh "$image_bundle" || true

          started="$SECONDS"
          hdiutil compact "$image_bundle"
          duration="$((SECONDS - started))"
          echo "NAMESPACE_ARTIFACT_IMAGE_COMPACTED path=$image_bundle durationSeconds=$duration"

          du -sh "$image_bundle" || true

          started="$SECONDS"
          rm -f "$image_archive"
          tar -cpf - -C "$image_parent" "$(basename "$image_bundle")" | zstd -T0 -1 > "$image_archive"
          duration="$((SECONDS - started))"
          echo "NAMESPACE_ARTIFACT_IMAGE_ARCHIVE_CREATED path=$image_archive format=sparsebundle-tar compression=zstd level=1 durationSeconds=$duration"
          ls -lh "$image_archive"

          started="$SECONDS"
          if upload_error="$(
            nsc artifact upload \
              "$image_archive" \
              "$NAMESPACE_NIX_STORE_ARTIFACT" \
              --expires_in "$NAMESPACE_NIX_STORE_ARTIFACT_EXPIRY" \
              2>&1
          )"; then
            duration="$((SECONDS - started))"
            echo "NAMESPACE_ARTIFACT_UPLOAD_COMPLETE path=$NAMESPACE_NIX_STORE_ARTIFACT expiresIn=$NAMESPACE_NIX_STORE_ARTIFACT_EXPIRY mode=nsc durationSeconds=$duration"
            exit 0
          fi

          if ! grep -Eiq 'already[ _-]?exists|conflict' <<< "$upload_error"; then
            unset upload_error
            echo "::error ::Namespace artifact upload failed without an existing-artifact conflict; retaining the current artifact."
            exit 1
          fi
          unset upload_error

          echo "Namespace artifact already exists; expiring it before one replacement attempt."
          nsc artifact expire "$NAMESPACE_NIX_STORE_ARTIFACT"

          started="$SECONDS"
          nsc artifact upload "$image_archive" "$NAMESPACE_NIX_STORE_ARTIFACT" --expires_in "$NAMESPACE_NIX_STORE_ARTIFACT_EXPIRY"
          duration="$((SECONDS - started))"
          echo "NAMESPACE_ARTIFACT_UPLOAD_COMPLETE path=$NAMESPACE_NIX_STORE_ARTIFACT expiresIn=$NAMESPACE_NIX_STORE_ARTIFACT_EXPIRY mode=nsc durationSeconds=$duration"
